---
apiVersion: v1
kind: Secret
metadata:
  name: backup-${APP_NAME}-secret
stringData:
  RESTIC_REPOSITORY: '{{ .B2_BUCKET_URL }}/${APP_NAME}'
  RESTIC_PASSWORD: '{{ .RESTIC_PASSWORD }}'
  AWS_ACCESS_KEY_ID: '{{ .B2_APPLICATION_KEY_ID }}'
  AWS_SECRET_ACCESS_KEY: '{{ .B2_APPLICATION_KEY }}'
