---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: "wildcard-private"
spec:
  secretName: "wildcard-tls-private"
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "*.${INTERNAL_DOMAIN}"
