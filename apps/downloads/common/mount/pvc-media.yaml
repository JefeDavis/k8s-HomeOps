---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-downloads-media-pvc
  namespace: downloads
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: media
  resources:
    requests:
      storage: 1Mi
