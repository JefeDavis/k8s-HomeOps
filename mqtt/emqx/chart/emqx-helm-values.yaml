apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: emqx-helm-values
  namespace: mqtt
spec:
  keys:
    - values.yaml
  path: secrets/mqtt/emqx/chart/emqx-helm-values
  type: Opaque
