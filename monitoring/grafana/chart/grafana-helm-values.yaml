apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: grafana-helm-values
  namespace: monitoring
spec:
  keys:
    - values.yaml
  path: secrets/monitoring/grafana/chart/grafana-helm-values
  type: Opaque
