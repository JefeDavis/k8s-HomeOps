---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tdarr-cache
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
  storageClassName: ceph-filesystem
