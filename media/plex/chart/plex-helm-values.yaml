apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: plex-helm-values
  namespace: media
spec:
  keys:
    - values.yaml
  path: secrets/media/plex/chart/plex-helm-values
  type: Opaque
